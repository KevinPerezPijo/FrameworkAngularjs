// app-routing.module.ts
// Archivo generado automaticamente

import { NgModule } from '@angular/core'; // Importando el decorador NgModule
import { RouterModule, Routes } from '@angular/router'; // Importando el modulo RouterModule para los servicios necesarios para la navegacion y la interfaz Routes para las rutas de la aplicacion

const routes: Routes = []; // Definiendo la constante arreglo vacio Routes

@NgModule({
  imports: [RouterModule.forRoot(routes)], // Definiendo modulo raiz 
  exports: [RouterModule]
})
export class AppRoutingModule { } // Clase que puede ser utilizada por otros archivos TypeScript
